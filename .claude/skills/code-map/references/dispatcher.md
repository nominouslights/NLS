<!-- codemap-hash: dfa74d20b83b8344060545b34364c4a13c85077b @ 9c53d7b 2026-09-21 -->
<!-- Generated by codemap.mjs — edit only inside notes blocks. Regenerate: node .claude/skills/code-map/scripts/codemap.mjs generate -->

# Code Map — Dispatcher (Admin Web App / Dispatch Console)

Next.js 16 / React 19, mostly API-backed with mock remnants. components/Console.tsx is a
two-level shell: the Home.tsx launcher, then one apps/*App at a time, each rendering NavRail
with its own group (manifest in lib/apps.ts; lib/nav.ts derives NAV_GROUPS from it). Styling
is inline style objects driven by lib/theme.ts tokens — no Tailwind/CSS modules.

## Screens — Dispatcher/components/screens (18 top-level)
<!-- gen:dispatcher-screens:start -->
| Screen | ~Lines |
|---|---|
| Billing.tsx | ~2100 — LARGE, read in targeted slices |
| BookingPolicy.tsx | ~0 |
| Bookings.tsx | ~1300 — LARGE, read in targeted slices |
| Cargo.tsx | ~100 |
| Clients.tsx | ~300 |
| Communications.tsx | ~300 |
| DispatchBoard.tsx | ~300 |
| Drivers.tsx | ~1000 — LARGE, read in targeted slices |
| Fleet.tsx | ~300 |
| Incidents.tsx | ~100 |
| LiveMap.tsx | ~300 |
| Manifests.tsx | ~500 |
| Reports.tsx | ~100 |
| Riders.tsx | ~400 |
| RoutesSchedules.tsx | ~1600 — LARGE, read in targeted slices |
| Settings.tsx | ~400 |
| Stops.tsx | ~300 |
| Trips.tsx | ~2700 — LARGE, read in targeted slices |
Sub-screen folders: screens/cargo/ (4), screens/clients/ (7), screens/drivers/ (4), screens/fleet/ (14), screens/reports/ (3), screens/settings/ (1)
<!-- gen:dispatcher-screens:end -->
<!-- notes:dispatcher-screens:start -->
- BookingPolicy.tsx is a 30-line page frame around screens/settings/BookingPolicyPanel.tsx, moved out of Settings.tsx (now five tabs — Budget Codes left too, it lives in the Budgeting console).
- Reports.tsx is a ~60-line shell switching between screens/reports/ClientAccruals.tsx and TerminusSummary.tsx; the report tab bar and the pseudo-table cellStyle/headerRowStyle both live in screens/reports/shared.tsx.
<!-- notes:dispatcher-screens:end -->

## Components — Dispatcher/components
<!-- gen:dispatcher-components:start -->
app/: globals.css, layout.tsx, page.tsx
Top-level: AuthGate, ClearanceFormModal, ClientContactFormModal, ClientOnboardingWizard, Console, ContactFormModal, ContractFormModal, CreateTripWizard, CredentialFormModal, CredentialImageLightbox, CredentialPhotoModal, DocumentUploadModal, DriverFormModal, EmailTemplateModal, HeaderClock, Home, HosLogEntryModal, InspectionEntryModal, InteractionModal, LeaveFormModal, LoginScreen, ManifestEditorModal, NavRail, PoFormModal, RecordPmCompletionModal, RetirementCertificateModal, SendAccrualsEmailModal, SendPickupEmailModal, ServiceRecordModal, SetupScreen, SpecialDatesModal, StopFormModal, TopBar, TripInspectionModal, VehicleFormModal, WorkOrderModal
ui/ (13): Button, Chip, Chip.test, CorridorStepper, Field, FileField, ImageUploadField, MetricTile, ModalShell, MonthGrid, Pager, Panel, PeriodNav
apps/ (8): AdminApp, CommercialApp, CommunityBookingApp, DriversApp, FleetApp, TodayApp, TripOpsApp, shell
fleet/ (4): DefectsPanel, InspectionDetailModal, PmPlansModal, ShopRegistryModal
inspection/ (1): ChecklistGroupEditor
manifest/ (3): PassengerCsvImport, RiderPickerModal, manifestRows
<!-- gen:dispatcher-components:end -->
<!-- notes:dispatcher-components:start -->
- Console.tsx is a two-level shell: Home.tsx (the launcher tile grid, gated per role by lib/apps.ts) → one apps/*App.tsx at a time, each rendering NavRail with its own group and screens; only tripSelId stays hoisted in Console, everything else is per-app via lib/appState.ts. apps/shell.ts is the Shell/AppProps contract every app takes; NavRail's `groups`/`onHome` and TopBar's `onHome`/`app` (breadcrumb) props exist for this.
- fleet/DefectsPanel.tsx is one component rendered on four surfaces (trip detail, CreateTripWizard, the vehicle's Open Defects tab, and its ResolveDefectModal alone from InspectionDetailModal) — WARN-ONLY by design: it reports nothing to its parent, so no mount point can quietly become a block on trip creation or vehicle assignment.
- ui/Chip.tsx StatusChip takes an optional `glyph` to separate two states that share one protected colour (a defect's Major and Out-of-Service are both vermillion) — an extra icon, never a new colour.
<!-- notes:dispatcher-components:end -->

## Lib — Dispatcher/lib (67 files)
<!-- gen:dispatcher-lib:start -->
api/ (15): billing, booking, clients, drivers, fleet, format, maintenance, notifications, pm, riders, shared, shipments, stops, transport, trips
documents/ (PDF generators): accrualsPdf, invoicePdf, terminusPdf, tripManifestPdf, workOrderPdf (each index/sections/styles)
Top-level: api, appState, apps, auth, claims, clientStore, clipboard, company, data, driverCompliance, driverStore, format, googleMaps, hosRules.test, hosRules, inspectionWorkOrder, maintenanceStore, nav, period.test, period, pmDisplay, roles, schedule.test, schedule, theme.test, theme, timetable, tripManifestChecklist, types, useToday, workOrderDisplay
billing/ (2)
manifest/ (1)
reports/ (1)
<!-- gen:dispatcher-lib:end -->
<!-- notes:dispatcher-lib:start -->
- data.ts holds ALL mock data (the API seam — never invent endpoint shapes) · types.ts entity types · theme.ts color/typography tokens (extend it, no ad hoc colors) · nav.ts now holds only the ScreenId/NavItem/NavGroup types — NAV_GROUPS is derived from apps.ts, so a screen changes apps by editing the manifest, never nav.ts.
- apps.ts is the app manifest (APPS, canUseApp, appForScreen, railGroupsFor, appBadge): one `internal` app per NavRail group, plus the Budgeting tile as an `external` link whose href is the build-time NEXT_PUBLIC_BUDGETING_URL (unset renders "Not configured" rather than hiding the tile); it may only `import type` from nav.ts or the derivation becomes a cycle.
- roles.ts mirrors Backend/src/Shared/Kernel/Roles.cs (DISPATCH_ROLES, BUDGET_ROLES; legacy "Admin" normalises to Owner, matching is case-sensitive) — a UX gate for launcher tiles, not a boundary · appState.ts useRetainedState is a module-level Map so an app's list position survives the unmount that going Home causes; Console clears it on sign-out.
- billing/accruals.ts and reports/terminus.ts are the pure period→figures report derivations; the screen, the printed sheet (documents/accrualsPdf, documents/terminusPdf) and the clipboard export all consume them rather than recompute.
- claims.ts decodes the JWT payload WITHOUT signature verification — deliberate UX gate, not a security boundary (the API validates every request); claim names couple to Identity's JwtAccessTokenIssuer.cs, and Budgeting/lib/claims.ts is a twin that must stay in step.
<!-- notes:dispatcher-lib:end -->
