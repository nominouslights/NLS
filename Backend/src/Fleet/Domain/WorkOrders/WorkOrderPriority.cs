namespace NorthernLink.Fleet.Domain.WorkOrders;

/// <summary>Work-order priority (drives the UI chip colour).</summary>
public enum WorkOrderPriority
{
    Low,
    Medium,
    High,
    Critical,
}
