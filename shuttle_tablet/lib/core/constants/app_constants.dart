class AppConstants {
  /// Dispatch office phone — update with your actual number.
  static const String dispatchPhone = '+12041234567';

  /// Default site/client contact phone — update per-client or leave as fallback.
  static const String siteContactPhone = '+12049876543';
}
